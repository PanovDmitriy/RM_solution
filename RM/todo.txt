1. заменить set_begin_state на добавление линка из ничего в начальное состоние и из конечного состояния в ничего
2. продумать как иницировать state.activate если event не может быть null
3. изменить set_status с учётом п.п. 1, 2 и так что б пауза и стоп обрабатывали internal action текущего состояния
4. предопределённые асинхронные функции internal action: timer (генерирует local_time_event), wait_for (генерирует local_time_event), start (генерирует local_change_event для initial state), observer (генерирует local_change_event)

сделать проверку целостности опредения автомата: прверка наличия initial states, связей и т.д. data consistency

transition
auto_transition - потомок или предок transition? и вообще, нужен ли он или это обычный переход, а авто опредялется способом добавления в автомат? должен быть ЗАПРЕТ на переопределение граничного условия - всегда true! 
переход триггерный и нетриггерный !

decltype

local_change_event, local_time_event - может просто свести к предопределённым идентификаторам, а не типам? ТПЗ! Ворпос на засыпку: а возможны ли локальные события, кроме предопределённых? и как упраляться с разделением по типам локал-голобал - rtti?
как реализовать авто-переход по локальным событиям? может только через предопределённые функции интернал экшн? т.е. не в событиях сделать разделение, а в предопределённых фциях?


